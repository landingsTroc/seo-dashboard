name: Refresh SEO data

on:
  schedule:
    # Once daily. The Ahrefs API is metered per unit and each full run costs
    # ~1,800 units against a 100,000/month workspace quota, so anything more
    # frequent exhausts it well before the reset date. SEO metrics do not move
    # meaningfully within a day. GitHub cron is UTC and best-effort.
    # Push events deploy but do NOT re-fetch Ahrefs — see AHREFS_SKIP below.
    - cron: "0 6 * * *"
  workflow_dispatch: {}   # lets you refresh on demand from the Actions tab
  push:
    branches: [main]
    paths:
      - "scripts/**"
      - "index.html"
      - "reports/**"          # publishing a report must deploy it, not wait for the next cron
      - ".github/workflows/refresh.yml"

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: seo-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      # A failed refresh must never block publishing. If Ahrefs is rate-limited or
      # down, the site still deploys with the last good data and the dashboard
      # shows how stale it is.
      - name: Fetch Ahrefs + PageSpeed data
        continue-on-error: true
        env:
          # Secrets are injected here and never reach the published page.
          AHREFS_API_TOKEN: ${{ secrets.AHREFS_API_TOKEN }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          # Full service-account JSON key. A plain API key cannot read Search
          # Console — the API rejects it with 401.
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          TARGET_DOMAIN: trocglobal.com
          # Ahrefs is metered (~1,800 units/run, 100k per month for the whole
          # workspace). A push only means the dashboard code changed, not that
          # the data did, so deploy runs reuse the last snapshot. The daily cron
          # and a manual "Run workflow" still fetch fresh data.
          AHREFS_SKIP: ${{ github.event_name == 'push' && '1' || '0' }}
        run: node scripts/fetch-data.mjs

      - name: Commit refreshed data
        run: |
          git config user.name "seo-bot"
          git config user.email "seo-bot@users.noreply.github.com"
          # data/latest.json is regenerated wholesale every run, so a concurrent
          # run's version is never worth merging — ours is simply newer. Rebasing
          # two rewrites of the same generated file only produces conflicts, so
          # reset onto the current remote tip and re-commit our fresh copy instead.
          for attempt in 1 2 3; do
            git fetch -q origin "${GITHUB_REF_NAME}"
            git reset -q "origin/${GITHUB_REF_NAME}"   # keeps the working tree
            git add data/latest.json
            if git diff --staged --quiet; then
              echo "No change in SEO data — nothing to commit."
              exit 0
            fi
            git commit -q -m "chore: refresh SEO data $(date -u +%Y-%m-%dT%H:%MZ)"
            if git push -q; then
              echo "Pushed on attempt ${attempt}."
              exit 0
            fi
            echo "Push rejected (attempt ${attempt}) — another run landed first, retrying."
          done
          echo "::error::Could not push refreshed data after 3 attempts."
          exit 1

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: deployment
        uses: actions/deploy-pages@v4
